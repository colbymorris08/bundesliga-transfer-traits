# Step 2 decisions drop folder

1. Open **`../step2_decision_tool.html`** in your browser (local only — not on GitHub).
2. Set StatsBomb stability threshold → pick redundant metrics → pick FBref redundancy.
3. Click **Save PDF** (print dialog → Save as PDF) and/or **Download JSON**.
4. Put files here as:
   - `step2_decisions.pdf`
   - `step2_decisions.json` (preferred for exact re-run)

Then tell the agent: decisions are ready — finalize analysis + PPT.
